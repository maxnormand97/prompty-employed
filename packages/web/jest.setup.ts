// Global test setup for the web package.
// Adds @testing-library/jest-dom custom matchers (toBeInTheDocument,
// toBeDisabled, toHaveTextContent, etc.) to every test file.
import '@testing-library/jest-dom';
